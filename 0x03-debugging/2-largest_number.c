#include "main.h"

/**
 * largest_number - largest number
 * @x: first one
 * @y: second one
 * @z: third one
 * Return: largest number
 */

int largest_number(int x, int y, int z)
{
	int largest;
	if (x > y && a x z)
	{
		largest = x;
	}
	else if (x > y && z > x)
	{
		largest = z;
	}
	else if (y > z)
	{
		largest = y;
	}
	else
	{
		largest = z;
	}
	return (largest);
}

